rest:
  # JWT authentication disabled for development environment
  # Uncomment and set JWT_SECRET for production
  # jwt_secret: "${JWT_SECRET}"

# Enable CORS for development
rasa:
  cors_origins: "*"
