version: "3.1"

# NLU examples for the 'deny' intent
nlu:
  - intent: deny
    examples: |
      - no
      - n
      - never
      - I don't think so
      - don't like that
      - no way
      - not really
