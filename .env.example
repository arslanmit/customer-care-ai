# Example environment variables for Customer Care AI backend
# Copy this file to `.env` and replace values as needed

# JWT secret used to sign access tokens.
# MUST be a strong unpredictable string. Generate one with:
#   openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_SECRET

# Access token lifetime in seconds (optional)
# JWT_EXPIRE=3600
